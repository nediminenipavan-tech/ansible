- name: variables from filies
  hosts: local
  connection: local
  vars_files:
  - course.yaml
  tasks:
  - name: print course info
    ansible.builtin.debug:
      msg: "Course Name is:  {{ COURSE }}, Duration: {{ DURATION }}, Trainer: {{ Trainer }}" 